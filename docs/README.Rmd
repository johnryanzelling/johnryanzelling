# D:/Projects/Main/johnryanzelling

```{r}
library(fs)
current_dir <- getwd()
parent_dir <- dirname(current_dir)
#all_files <- list.files(parent_dir, full.names = TRUE, recursive = TRUE)
#print(all_files)
dir_tree(parent_dir, recurse=TRUE)

```

# TODO
NA

